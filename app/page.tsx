import { auth, signIn } from "@/auth";
import { redirect } from "next/navigation";
export default async function Home(){ const session=await auth(); if(session) redirect("/dashboard"); return <main className="shell"><nav className="topbar"><div className="brand">Coach<span>Log</span></div><form action={async()=>{"use server";await signIn("google",{redirectTo:"/dashboard"})}}><button>使用 Google 登入</button></form></nav><section className="hero"><div><div className="eyebrow">教練的訓練資料庫</div><h1>看見每一次進步的重量。</h1><p>集中管理學生、課程動作、身體組成與預約行程。資料只屬於登入的教練，從今天開始留下可追蹤的訓練紀錄。</p><form action={async()=>{"use server";await signIn("google",{redirectTo:"/dashboard"})}}><button>連結 Google 帳戶開始使用</button></form></div><div className="hero-card"><h2>本週訓練摘要</h2><div className="stat"><span>學生預約</span><strong>12 堂</strong></div><div className="stat"><span>深蹲平均進步</span><strong>+7.5 kg</strong></div><div className="stat"><span>完成紀錄</span><strong>94%</strong></div></div></section></main>}
