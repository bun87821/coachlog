"use client";import {useMemo,useState} from "react";import {LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer,Legend,CartesianGrid} from "recharts";
export function ProgressCharts({metrics,loads}:{metrics:any[],loads:any[]}){const names=useMemo(()=>Array.from(new Set(loads.map(row=>row.name))) as string[],[loads]);const [selected,setSelected]=useState(names[0]||"");const selectedLoads=loads.filter(row=>row.name===selected);return <div className="grid"><div className="card" style={{gridColumn:"span 2"}}><h3>身體組成趨勢</h3><div className="chart"><ResponsiveContainer><LineChart data={metrics}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date"/><YAxis/><Tooltip/><Legend/><Line type="monotone" dataKey="weight" name="體重 kg" stroke="#176b45"/><Line type="monotone" dataKey="bodyFat" name="體脂 %" stroke="#e47c45"/><Line type="monotone" dataKey="muscle" name="肌肉量 kg" stroke="#425fc7"/></LineChart></ResponsiveContainer></div></div><div className="card"><div className="chart-title"><h3>動作重量趨勢</h3>{names.length>0&&<select aria-label="選擇趨勢動作" value={selected} onChange={event=>setSelected(event.target.value)}>{names.map(name=><option key={name}>{name}</option>)}</select>}</div><div className="chart"><ResponsiveContainer><LineChart data={selectedLoads}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="date"/><YAxis/><Tooltip/><Legend/><Line type="monotone" dataKey="weight" name="最高重量（換算 kg）" stroke="#17211b"/></LineChart></ResponsiveContainer></div></div></div>}
